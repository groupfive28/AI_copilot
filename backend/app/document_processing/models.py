# SQLAlchemy models for the document processing layer.
#
# e.g. Document, UploadBatch, ProcessingJob.
# Left empty intentionally — this layer is scaffolding only, no tables yet.
# No persistence happens on upload in this prototype; see router.py.
#
# from app.core.database import Base
